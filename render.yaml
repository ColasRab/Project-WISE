services:
  - type: web
    name: weather-api
    env: python
    buildCommand: pip install -r python/requirements.txt
    startCommand: uvicorn python.weather_service:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health